# API endpoint used when syncing survey records to the server.
# Defaults to /api/surveys. For demo usage without a real backend, any 404 will fall back gracefully.
VITE_API_ENDPOINT=https://your-server.example.com/api/surveys
